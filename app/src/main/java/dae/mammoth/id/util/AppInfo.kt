package dae.mammoth.id

/**
 * Central app metadata, referenced across screens (credits, about, footer).
 * Keep in sync with the values in build.gradle.kts.
 */
object AppInfo {
    const val NAME = "Mammoth"
    const val TAGLINE = "Bot Runner · Pterodactyl-style"
    const val PACKAGE_ID = "dae.mammoth.id"
    const val VERSION_NAME = "1.0.0"
    const val VERSION_CODE = 2
}
